---
phase: 02-data-fetch
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - scripts/fetch_chorus.py
autonomous: true
requirements:
  - CHO-01
  - CHO-02

must_haves:
  truths:
    - "Meeting notes containing a chorus.ai/meeting/ID URL cause chorus_transcripts.json to contain that transcript (or a per-ID sentinel if 404/401)"
    - "Meeting notes without any Chorus URL produce chorus_transcripts.json with transcript_available=false and empty conversations list"
    - "A Chorus 404 or 401 does not raise an exception — the script exits 0 with the sentinel written"
    - "A Chorus timeout or connection error does not raise — same sentinel and exit 0"
    - "A 401 is logged as WARNING separately from a 404 to surface token expiry"
    - "On unrecovered failure (unexpected exception), write_dlq is called before SystemExit(1)"
  artifacts:
    - path: "scripts/fetch_chorus.py"
      provides: "Chorus ID regex extraction + transcript fetch with silent fallback sentinel"
      exports: ["main"]
      min_lines: 80
  key_links:
    - from: "$RUNNER_TEMP/hubspot_contact.json"
      to: "meeting_engagements list"
      via: "file_io.read_json('hubspot_contact.json') then data.get('meeting_engagements', [])"
      pattern: "read_json.*hubspot_contact"
    - from: "CHORUS_ID_PATTERN.findall on hs_meeting_body + hs_internal_meeting_notes"
      to: "chorus_ids list"
      via: "re.compile(r'chorus\\.ai/meeting/([\\w-]+)') applied to each meeting field"
      pattern: "chorus.ai/meeting"
    - from: "fetch_chorus_transcripts result dict"
      to: "$RUNNER_TEMP/chorus_transcripts.json"
      via: "file_io.write_json('chorus_transcripts.json', result)"
      pattern: "write_json.*chorus_transcripts"
---

<objective>
Implement scripts/fetch_chorus.py — the Chorus AI transcript fetch script that replaces the Phase 1 stub.

Purpose: The ASOTOS prompt requires Chorus call intelligence when available. This script reads hubspot_contact.json (written by Plan 02-01), extracts Chorus conversation IDs from meeting notes, fetches transcripts, and writes chorus_transcripts.json. Critically, every Chorus failure mode — 404, 401, timeout, connection error — must produce a silent sentinel and exit 0. The pipeline must never fail because Chorus is unavailable.

Output: scripts/fetch_chorus.py that writes $RUNNER_TEMP/chorus_transcripts.json as either the full transcript payload or the explicit sentinel {"transcript_available": false, "conversations": []}.
</objective>

<execution_context>
@C:\Users\irahfo\Outreach\Asotos\.planning\phases\02-data-fetch\02-RESEARCH.md
</execution_context>

<context>
@C:\Users\irahfo\Outreach\Asotos\.planning\ROADMAP.md
@C:\Users\irahfo\Outreach\Asotos\CLAUDE.md

<interfaces>
<!-- Contracts the executor needs. Extracted from Phase 1 and Plan 02-01 outputs. -->

From scripts/lib/api_client.py (Phase 1):
  chorus_retry — tenacity decorator; retries requests.HTTPError with status_code in (429, 500, 502, 503, 504)
  Usage: @chorus_retry on _fetch_single_transcript function

From scripts/lib/file_io.py (Phase 1):
  write_json(filename: str, data: dict) -> None  — writes to RUNNER_TEMP/filename
  read_json(filename: str) -> dict               — raises FileNotFoundError if absent

From scripts/lib/dlq_writer.py (Phase 1):
  write_dlq(contact_id: str, contact_email: str, failed_step: str, error_message: str) -> None

hubspot_contact.json produced by Plan 02-01 (relevant fields):
  {
    "meeting_engagements": [
      {
        "id": str,
        "hs_meeting_body": str,           # may contain "https://chorus.ai/meeting/abc123"
        "hs_internal_meeting_notes": str, # may also contain Chorus URLs
        ...
      }
    ]
  }

Chorus auth (CLAUDE.md — NON-NEGOTIABLE):
  Authorization header value = raw token value from CHORUS_API_TOKEN env var
  NO "Bearer " prefix, NO "Token " prefix
  Example: headers = {"Authorization": os.environ.get("CHORUS_API_TOKEN", "")}

Chorus endpoint (RESEARCH.md Pattern 9, STACK.md):
  GET https://chorus.ai/api/v3/engagements/{conversation_id}
  timeout=30 (seconds)

chorus_transcripts.json output schema:
  Sentinel (no IDs found, or all fetches failed):
    {"transcript_available": false, "conversations": []}

  Success (at least one transcript fetched):
    {
      "transcript_available": true,
      "conversations": [
        {
          "conversation_id": str,
          "meeting_title": str,
          "date_time": str,
          "transcript": str,
          "available": true
        },
        ...
      ]
    }

  Per-ID failure (within a mostly-successful response):
    {"conversation_id": str, "available": false, "error_status": str_or_int}
    included in conversations list alongside successful items

Environment variables consumed:
  CONTACT_ID          — used in DLQ only
  CONTACT_EMAIL       — used in DLQ only
  CHORUS_API_TOKEN    — raw Chorus token (no prefix); empty string if absent
  RUNNER_TEMP         — runner temp dir (via file_io)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement fetch_chorus.py with regex extraction, transcript fetch, and sentinel fallback</name>
  <files>scripts/fetch_chorus.py</files>
  <read_first>
    - C:\Users\irahfo\Outreach\Asotos\.planning\phases\02-data-fetch\02-RESEARCH.md (Pattern 9 — complete Chorus code; Pitfall 4 on Chorus ID regex; Pitfall 6 on 401 vs 404 logging distinction; Anti-Patterns; Open Questions 1 on endpoint validity)
    - C:\Users\irahfo\Outreach\Asotos\CLAUDE.md (Chorus AI section — auth header format, silent fallback rule, regex pattern; DLQ Pattern; Known Risks item 3 on Chorus endpoint uncertainty)
  </read_first>
  <action>
    Create scripts/fetch_chorus.py. The script replaces the Phase 1 stub. Use RESEARCH.md Pattern 9 code as the base, with the following exact structure.

    FILE HEADER (exact):
      #!/usr/bin/env python3
      """fetch_chorus.py — Phase 2: extract Chorus AI transcript IDs from meeting notes and fetch transcripts."""

    IMPORTS (in this order):
      import os, sys, re, logging
      import requests
      sys.path.insert(0, os.path.join(os.path.dirname(__file__), "lib"))
      from api_client import chorus_retry
      from dlq_writer import write_dlq
      from file_io import write_json, read_json

    NOTE: requests is imported at the top level here (not lazily) because this script ONLY uses requests for Chorus; there is no conditional import concern. The lazy import in api_client.py is for the retry decorator's type check only.

    MODULE-LEVEL CONSTANTS:
      CONTACT_ID = os.environ.get("CONTACT_ID", "")
      CONTACT_EMAIL = os.environ.get("CONTACT_EMAIL", "")
      SENTINEL = {"transcript_available": False, "conversations": []}
      CHORUS_ID_PATTERN = re.compile(r"chorus\.ai/meeting/([\w-]+)")

    Note on regex: The pattern uses [\w-]+ (includes hyphen) per RESEARCH.md Pitfall 4 to handle potential ID format variants. CLAUDE.md shows (\w+) as the base — the hyphen extension is additive and non-breaking.

    FUNCTION: extract_chorus_ids(meeting_engagements: list) -> list[str]
      Extract unique Chorus conversation IDs from meeting note fields, preserving order.
      For each meeting in meeting_engagements:
        props = meeting if isinstance(meeting, dict) else (meeting.properties if hasattr(meeting, 'properties') else {})
        For each field in ["hs_meeting_body", "hs_internal_meeting_notes"]:
          text = props.get(field) or ""
          ids.extend(CHORUS_ID_PATTERN.findall(text))
      Use dict.fromkeys(ids) to deduplicate while preserving insertion order.
      Return list(dict.fromkeys(ids)).
      If no IDs found: log at DEBUG level: logging.debug("[fetch_chorus] no Chorus IDs found in meeting notes")

    FUNCTION: _fetch_single_transcript(conversation_id: str, token: str) -> dict
      Decorated with @chorus_retry.
      This is the retryable inner function — chorus_retry handles 429/5xx only.
      headers = {
          "Authorization": token,   # Raw token — NO prefix (CLAUDE.md — NON-NEGOTIABLE)
          "Content-Type": "application/json",
      }
      url = f"https://chorus.ai/api/v3/engagements/{conversation_id}"
      resp = requests.get(url, headers=headers, timeout=30)
      resp.raise_for_status()   # Must be called — chorus_retry fires on HTTPError (RESEARCH.md Anti-Patterns)
      return resp.json()

    FUNCTION: fetch_chorus_transcripts(chorus_ids: list[str], token: str) -> dict
      If not chorus_ids: return SENTINEL (transcript_available=False, conversations=[]).
      conversations = []
      any_success = False
      For each cid in chorus_ids:
        try:
          data = _fetch_single_transcript(cid, token)
          conversations.append({
              "conversation_id": cid,
              "meeting_title": data.get("title") or data.get("meeting_title") or "",
              "date_time": data.get("date_time") or data.get("dateTime") or "",
              "transcript": data.get("transcript") or "",
              "available": True,
          })
          any_success = True
        except requests.exceptions.HTTPError as e:
          status = e.response.status_code if e.response is not None else "unknown"
          if status == 401:
              # 401 means token may be invalid for ALL calls — surface this prominently
              print(f"[fetch_chorus] WARNING: Chorus auth failure (401) for conversation_id={cid} — token may be invalid for all Chorus calls", flush=True)
          elif status == 404:
              print(f"[fetch_chorus] INFO: Chorus conversation {cid} not found (404) — skipping", flush=True)
          else:
              print(f"[fetch_chorus] INFO: Chorus HTTP error {status} for conversation_id={cid} — skipping", flush=True)
          conversations.append({"conversation_id": cid, "available": False, "error_status": status})
        except requests.exceptions.Timeout:
          print(f"[fetch_chorus] INFO: Chorus request timeout for conversation_id={cid} — skipping", flush=True)
          conversations.append({"conversation_id": cid, "available": False, "error_status": "timeout"})
        except Exception as e:
          print(f"[fetch_chorus] INFO: Chorus connection error for conversation_id={cid} — skipping", flush=True)
          conversations.append({"conversation_id": cid, "available": False, "error_status": "connection_error"})
      return {
          "transcript_available": any_success,
          "conversations": conversations,
      }

    FUNCTION: main()
      Per RESEARCH.md "Complete fetch_chorus.py Script Structure":
        print(f"[fetch_chorus] processing contact_id={CONTACT_ID}", flush=True)
        hubspot_data = read_json("hubspot_contact.json")
        meetings = hubspot_data.get("meeting_engagements", [])
        chorus_ids = extract_chorus_ids(meetings)
        print(f"[fetch_chorus] found {len(chorus_ids)} Chorus IDs", flush=True)
        if not chorus_ids:
            write_json("chorus_transcripts.json", SENTINEL)
            print("[fetch_chorus] no Chorus IDs — wrote sentinel", flush=True)
            return
        token = os.environ.get("CHORUS_API_TOKEN", "")
        if not token:
            print("[fetch_chorus] WARNING: CHORUS_API_TOKEN not set — writing sentinel", flush=True)
            write_json("chorus_transcripts.json", SENTINEL)
            return
        result = fetch_chorus_transcripts(chorus_ids, token)
        write_json("chorus_transcripts.json", result)
        print(f"[fetch_chorus] transcript_available={result['transcript_available']}, "
              f"conversations={len(result['conversations'])}", flush=True)

    if __name__ == "__main__" guard:
      try: main()
      except Exception as e:
        # DLQ + sentinel both written — downstream Phase 3 can still run with no transcripts
        write_dlq(CONTACT_ID, CONTACT_EMAIL, "fetch_chorus", str(e))
        write_json("chorus_transcripts.json", SENTINEL)
        raise SystemExit(1)

    NOTE on DLQ + sentinel dual-write: The sentinel is written in the __main__ guard's except clause so that even if an unexpected exception occurs (e.g., FileNotFoundError on read_json because hubspot_contact.json is absent), chorus_transcripts.json is still present for Phase 3. This differs from other scripts where only DLQ is written before raising.

    SECURITY RULES:
    - Never log the CHORUS_API_TOKEN value; log only status codes and conversation IDs
    - Log contact_id in debug prints; do not log contact_email in stdout
    - The Authorization header value comes directly from the env var — do not log it or repr() any requests.Request object that contains it
  </action>
  <verify>
    <automated>cd /mnt/c/Users/irahfo/Outreach/Asotos && python -c "
import ast, sys
with open('scripts/fetch_chorus.py') as f:
    source = f.read()
# Syntax check
try:
    ast.parse(source)
    print('syntax OK')
except SyntaxError as e:
    print(f'SYNTAX ERROR: {e}')
    sys.exit(1)
# Required functions
required = ['extract_chorus_ids', '_fetch_single_transcript', 'fetch_chorus_transcripts', 'main']
for fn in required:
    assert fn in source, f'Missing function: {fn}'
print('all required functions present')
# Chorus auth — must NOT have Bearer prefix in headers
assert 'Bearer' not in source, 'ERROR: Bearer prefix found in auth header — must be raw token only'
assert 'Authorization' in source, 'Missing Authorization header'
print('auth header: no Bearer prefix (correct)')
# endpoint
assert 'chorus.ai/api/v3/engagements' in source, 'Missing Chorus endpoint'
print('Chorus endpoint present')
# sentinel constant
assert 'transcript_available' in source, 'Missing transcript_available key'
assert 'SENTINEL' in source, 'Missing SENTINEL constant'
print('SENTINEL constant present')
# DLQ guard
assert 'write_dlq' in source, 'Missing write_dlq call'
assert 'SystemExit(1)' in source, 'Missing SystemExit(1)'
print('DLQ guard present')
# Sentinel written in __main__ except (dual-write)
import re
main_guard = source[source.find('__name__'):]
assert 'write_json' in main_guard and 'SENTINEL' in main_guard, 'Sentinel not written in __main__ except block'
print('dual-write (DLQ + sentinel) in __main__ guard: OK')
# 401 logged differently from 404
assert '401' in source, '401 status check missing'
print('401 vs 404 distinction present')
# No hardcoded RUNNER_TEMP path
assert '/home/runner' not in source, 'Hardcoded runner path found'
assert 'write_json' in source, 'write_json call missing'
print('file_io pattern OK')
# raise_for_status called (required for chorus_retry to fire on 5xx)
assert 'raise_for_status' in source, 'raise_for_status() missing — chorus_retry will not fire on 5xx'
print('raise_for_status() present')
print('Task 1 verify PASSED')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - scripts/fetch_chorus.py passes ast.parse (no syntax errors)
    - All 4 functions present: extract_chorus_ids, _fetch_single_transcript, fetch_chorus_transcripts, main
    - @chorus_retry decorator applied to _fetch_single_transcript only (not to fetch_chorus_transcripts — the outer function must not retry across IDs)
    - CHORUS_ID_PATTERN uses [\w-]+ (includes hyphen for variant ID formats)
    - Authorization header value is raw token with NO "Bearer" or "Token" prefix
    - resp.raise_for_status() called immediately after requests.get() inside _fetch_single_transcript
    - 401 logged with WARNING distinguishing token invalidity; 404 logged with INFO as expected skip
    - Both 401 and 404 produce {"conversation_id": cid, "available": False, "error_status": status} entry in conversations — neither raises an exception
    - requests.exceptions.Timeout caught explicitly as a distinct case (not caught only by the broad except Exception)
    - SENTINEL constant defined at module level as {"transcript_available": False, "conversations": []}
    - Sentinel written in BOTH: the if-not-chorus-ids early return AND the __main__ guard except block
    - write_dlq called before SystemExit(1) in __main__ guard except block
    - No CHORUS_API_TOKEN value logged anywhere in the script
  </acceptance_criteria>
  <done>scripts/fetch_chorus.py exists, passes syntax and structure checks, implements all CHO-01 and CHO-02 requirements with correct auth header, correct fallback behaviour, and correct sentinel output</done>
</task>

<task type="auto">
  <name>Task 2: Smoke-test sentinel paths and validate chorus_transcripts.json schema</name>
  <files>scripts/fetch_chorus.py</files>
  <read_first>
    - C:\Users\irahfo\Outreach\Asotos\.planning\phases\02-data-fetch\02-RESEARCH.md (Open Questions 1 — Chorus endpoint validity; Pitfall 6 — 401 vs 404 handling; Assumptions Log A4 — endpoint may have changed)
    - C:\Users\irahfo\Outreach\Asotos\CLAUDE.md (Known Risks item 3 — Chorus endpoint uncertainty)
  </read_first>
  <action>
    This task validates the four sentinel paths and the live transcript path for fetch_chorus.py, and fixes any issues found.

    STEP 1 — Test sentinel path: no Chorus IDs in meeting notes.
    Create a fixture hubspot_contact.json with meetings that have no Chorus URLs:
      python -c "
      import json, os, tempfile
      tmp = tempfile.mkdtemp()
      fixture = {
          'meeting_engagements': [
              {'id': '1', 'hs_meeting_body': 'No chorus link here', 'hs_internal_meeting_notes': ''},
              {'id': '2', 'hs_meeting_body': '', 'hs_internal_meeting_notes': 'Also no link'},
          ]
      }
      with open(os.path.join(tmp, 'hubspot_contact.json'), 'w') as f:
          json.dump(fixture, f)
      os.environ['RUNNER_TEMP'] = tmp
      os.environ['CONTACT_ID'] = 'test001'
      os.environ['CONTACT_EMAIL'] = 'test@example.com'
      import subprocess, sys
      r = subprocess.run([sys.executable, 'scripts/fetch_chorus.py'], capture_output=True, text=True,
                         env={**os.environ, 'RUNNER_TEMP': tmp})
      assert r.returncode == 0, f'Exited {r.returncode}: {r.stderr}'
      with open(os.path.join(tmp, 'chorus_transcripts.json')) as f:
          data = json.load(f)
      assert data == {'transcript_available': False, 'conversations': []}, f'Wrong sentinel: {data}'
      print('PASS: no-IDs sentinel path OK')
      "

    STEP 2 — Test sentinel path: Chorus IDs found but CHORUS_API_TOKEN not set.
      Same fixture but with a meeting containing a Chorus URL; run with CHORUS_API_TOKEN unset.
      Verify exit code = 0 and chorus_transcripts.json = SENTINEL.

    STEP 3 — Test 404/401 fallback path using requests_mock or a local HTTP stub.
    If requests_mock is available (pip show requests-mock):
      python -c "
      import json, os, tempfile, sys
      sys.path.insert(0, 'scripts/lib')
      os.environ['CONTACT_ID'] = 'test002'
      os.environ['CONTACT_EMAIL'] = 'test@example.com'

      import requests_mock as rm_module
      import requests
      from api_client import chorus_retry
      from file_io import write_json, read_json

      tmp = tempfile.mkdtemp()
      os.environ['RUNNER_TEMP'] = tmp
      sys.path.insert(0, 'scripts')
      # Import the module functions directly
      import importlib.util, types
      spec = importlib.util.spec_from_file_location('fetch_chorus', 'scripts/fetch_chorus.py')
      mod = importlib.util.load_from_spec(spec)
      spec.loader.exec_module(mod)

      with rm_module.Mocker() as m:
          m.get('https://chorus.ai/api/v3/engagements/abc123', status_code=404)
          result = mod.fetch_chorus_transcripts(['abc123'], 'fake-token')
      assert result['transcript_available'] == False
      assert result['conversations'][0]['available'] == False
      assert result['conversations'][0]['error_status'] == 404
      print('PASS: 404 fallback path OK')

      with rm_module.Mocker() as m:
          m.get('https://chorus.ai/api/v3/engagements/def456', status_code=401)
          result = mod.fetch_chorus_transcripts(['def456'], 'fake-token')
      assert result['transcript_available'] == False
      assert result['conversations'][0]['error_status'] == 401
      print('PASS: 401 fallback path OK')
      "
    If requests_mock is NOT available, test the 404/401 path by temporarily pointing the script to a local netcat listener or by mocking requests.get in-process:
      python -c "
      import sys, os, json, tempfile, unittest.mock as mock
      sys.path.insert(0, 'scripts/lib')
      sys.path.insert(0, 'scripts')
      tmp = tempfile.mkdtemp()
      os.environ['RUNNER_TEMP'] = tmp
      os.environ['CONTACT_ID'] = 'test003'
      os.environ['CONTACT_EMAIL'] = 'test@example.com'
      import importlib.util
      spec = importlib.util.spec_from_file_location('fetch_chorus', 'scripts/fetch_chorus.py')
      mod = importlib.util.load_from_spec(spec)
      spec.loader.exec_module(mod)

      # Mock a 404 response
      mock_resp = mock.MagicMock()
      mock_resp.status_code = 404
      mock_resp.raise_for_status.side_effect = __import__('requests').HTTPError(response=mock_resp)
      with mock.patch('requests.get', return_value=mock_resp):
          result = mod.fetch_chorus_transcripts(['abc123'], 'fake-token')
      assert result['transcript_available'] == False, f'Expected False, got {result}'
      assert result['conversations'][0]['error_status'] == 404
      print('PASS: mocked 404 path OK')
      "

    STEP 4 — Validate chorus_transcripts.json schema.
    Run the schema check against the sentinel output from Step 1:
      The file must parse as JSON without error.
      If transcript_available is False, conversations must be a list (may be empty).
      If transcript_available is True, each item in conversations must have conversation_id and available keys.

    STEP 5 — If CHORUS_API_TOKEN is available in the environment and a known conversation ID exists in the test contact's meeting notes (from Plan 02-01 smoke test), run the full live path:
      python scripts/fetch_chorus.py (with RUNNER_TEMP pointing to the directory containing the hubspot_contact.json from Plan 02-01)
      Verify chorus_transcripts.json is written and is valid JSON.
      If the endpoint returns an unexpected response shape (e.g., the transcript field is nested differently post-ZoomInfo), adjust the data.get() calls in fetch_chorus_transcripts to handle the actual response structure. Document the actual response shape in the SUMMARY.

    STEP 6 — Fix any issues found. Common issues:
      - If the Chorus endpoint returns 404 for a known valid meeting: the endpoint may have changed post-ZoomInfo acquisition. Update the URL in _fetch_single_transcript and note the new endpoint in the SUMMARY for future reference.
      - If the Authorization header format causes 401 on a known-valid token: try "Token {token}" and "Bearer {token}" as alternatives and note which one works. Update CLAUDE.md if the auth format has changed.
      - If conversations list is missing from the output: ensure the SENTINEL constant is defined correctly and the early return path writes it.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/irahfo/Outreach/Asotos && python -c "
import json, os, sys, tempfile, subprocess, unittest.mock as mock

# Test 1: no-IDs sentinel
tmp1 = tempfile.mkdtemp()
fixture = {'meeting_engagements': [{'id': '1', 'hs_meeting_body': 'No chorus here', 'hs_internal_meeting_notes': ''}]}
with open(os.path.join(tmp1, 'hubspot_contact.json'), 'w') as f:
    json.dump(fixture, f)
env1 = {**os.environ, 'RUNNER_TEMP': tmp1, 'CONTACT_ID': 'test001', 'CONTACT_EMAIL': 'test@example.com'}
env1.pop('CHORUS_API_TOKEN', None)
r1 = subprocess.run([sys.executable, 'scripts/fetch_chorus.py'], capture_output=True, text=True, env=env1)
assert r1.returncode == 0, f'Test1 exit {r1.returncode}: {r1.stderr}'
with open(os.path.join(tmp1, 'chorus_transcripts.json')) as f:
    d1 = json.load(f)
assert d1 == {'transcript_available': False, 'conversations': []}, f'Test1 wrong output: {d1}'
print('PASS: no-IDs sentinel path')

# Test 2: IDs found but no token
tmp2 = tempfile.mkdtemp()
fixture2 = {'meeting_engagements': [{'id': '1', 'hs_meeting_body': 'chorus.ai/meeting/abc123', 'hs_internal_meeting_notes': ''}]}
with open(os.path.join(tmp2, 'hubspot_contact.json'), 'w') as f:
    json.dump(fixture2, f)
env2 = {**os.environ, 'RUNNER_TEMP': tmp2, 'CONTACT_ID': 'test002', 'CONTACT_EMAIL': 'test@example.com'}
env2.pop('CHORUS_API_TOKEN', None)
r2 = subprocess.run([sys.executable, 'scripts/fetch_chorus.py'], capture_output=True, text=True, env=env2)
assert r2.returncode == 0, f'Test2 exit {r2.returncode}: {r2.stderr}'
with open(os.path.join(tmp2, 'chorus_transcripts.json')) as f:
    d2 = json.load(f)
assert d2.get('transcript_available') == False, f'Test2 wrong output: {d2}'
print('PASS: IDs found, no token, sentinel path')

# Test 3: mocked 404 — fetch_chorus_transcripts returns sentinel without raising
sys.path.insert(0, 'scripts/lib')
tmp3 = tempfile.mkdtemp()
os.environ['RUNNER_TEMP'] = tmp3
os.environ['CONTACT_ID'] = 'test003'
os.environ['CONTACT_EMAIL'] = 'test@example.com'
import importlib.util
spec = importlib.util.spec_from_file_location('fetch_chorus', 'scripts/fetch_chorus.py')
mod = importlib.util.load_from_spec(spec)
spec.loader.exec_module(mod)
import requests as req_module
mock_resp_404 = mock.MagicMock()
mock_resp_404.status_code = 404
http_err = req_module.exceptions.HTTPError(response=mock_resp_404)
mock_resp_404.raise_for_status.side_effect = http_err
with mock.patch.object(req_module, 'get', return_value=mock_resp_404):
    result_404 = mod.fetch_chorus_transcripts(['abc123'], 'fake-token')
assert result_404['transcript_available'] == False, f'404: expected False, got {result_404}'
assert result_404['conversations'][0]['error_status'] == 404, f'404 status not recorded'
print('PASS: mocked 404 does not raise, correct sentinel output')

# Test 4: mocked 401 — should produce WARNING log and sentinel (not raise)
mock_resp_401 = mock.MagicMock()
mock_resp_401.status_code = 401
http_err_401 = req_module.exceptions.HTTPError(response=mock_resp_401)
mock_resp_401.raise_for_status.side_effect = http_err_401
with mock.patch.object(req_module, 'get', return_value=mock_resp_401):
    result_401 = mod.fetch_chorus_transcripts(['def456'], 'fake-token')
assert result_401['transcript_available'] == False, f'401: expected False, got {result_401}'
assert result_401['conversations'][0]['error_status'] == 401
print('PASS: mocked 401 does not raise, correct sentinel output')

# Test 5: schema validation on chorus_transcripts.json
for path, label in [(os.path.join(tmp1, 'chorus_transcripts.json'), 'no-IDs'),
                     (os.path.join(tmp2, 'chorus_transcripts.json'), 'no-token')]:
    with open(path) as f:
        d = json.load(f)
    assert 'transcript_available' in d, f'{label}: missing transcript_available'
    assert 'conversations' in d, f'{label}: missing conversations'
    assert isinstance(d['conversations'], list), f'{label}: conversations must be list'
print('PASS: chorus_transcripts.json schema valid on both sentinel outputs')

print('All Task 2 verification checks PASSED')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - No-IDs sentinel path: script exits 0; chorus_transcripts.json = {"transcript_available": false, "conversations": []}
    - No-token-with-IDs path: script exits 0; chorus_transcripts.json = sentinel (transcript_available=false)
    - Mocked 404: fetch_chorus_transcripts returns dict with transcript_available=False and conversations[0].error_status=404; no exception raised
    - Mocked 401: same structure with error_status=401; no exception raised; WARNING log distinguishable from INFO 404 log
    - chorus_transcripts.json always contains both transcript_available and conversations keys
    - Script exits 0 on all sentinel paths; only exits 1 on unexpected exceptions (not on Chorus API errors)
    - If live Chorus token available: script runs against real meeting data and produces valid chorus_transcripts.json
  </acceptance_criteria>
  <done>All four fallback paths (no IDs, no token, 404, 401) exit 0 with correct sentinel; chorus_transcripts.json passes schema validation; live path validated if credentials available</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| $RUNNER_TEMP/hubspot_contact.json → fetch_chorus.py | Input from prior script in same job; trusted (same runner) |
| fetch_chorus.py → Chorus AI REST | Outbound HTTPS to chorus.ai; response data is meeting transcript text |
| Chorus API response → chorus_transcripts.json | Transcript content written to file; consumed by Phase 3 token computation |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | CHORUS_API_TOKEN in log output | mitigate | Token value never logged or printed; only status codes and conversation IDs appear in stdout; requests.Request repr() never logged |
| T-03-02 | Information Disclosure | 401 response swallowed silently | mitigate | 401 logged with explicit WARNING message flagging potential token invalidity; operators can detect token expiry from the WARNING line rather than it being invisible in a generic silent fallback |
| T-03-03 | Denial of Service | Chorus fetch hangs indefinitely | mitigate | timeout=30 enforced on requests.get(); runner job timeout is the outer bound but 30s per call is the inner guard |
| T-03-04 | Tampering | Chorus conversation ID injected via meeting notes | accept | IDs extracted by regex on known-format HubSpot CRM data (Staff Domain's own records); ID is used only as a URL path segment in a GET request; no shell injection or SQL injection path; HubSpot CRM is the trusted data source |
| T-03-05 | Repudiation | No record of which Chorus IDs were attempted | accept | Each conversation_id is written to chorus_transcripts.json with available and error_status fields; this constitutes a per-ID audit trail within the output file |
</threat_model>

<verification>
After both tasks complete:
1. Run the no-IDs sentinel test: create fixture JSON with no Chorus URLs, run script, assert chorus_transcripts.json == {"transcript_available": false, "conversations": []} and exit code 0
2. Run the schema validation block from Task 2 verify — all five assertions must pass
3. Confirm "Bearer" does not appear in scripts/fetch_chorus.py: python -c "assert 'Bearer' not in open('scripts/fetch_chorus.py').read(); print('no Bearer prefix: OK')"
4. Confirm raise_for_status() is called in _fetch_single_transcript: python -c "assert 'raise_for_status' in open('scripts/fetch_chorus.py').read(); print('raise_for_status present: OK')"
5. Confirm dual-write in __main__ guard: grep for both "write_dlq" and "write_json" in the except block of the if __name__ == "__main__" section
6. If CHORUS_API_TOKEN is set in the environment and hubspot_contact.json from Plan 02-01 is available: run end-to-end and validate chorus_transcripts.json structure
</verification>

<success_criteria>
- scripts/fetch_chorus.py exists with all 4 required functions
- Script exits 0 on all Chorus failure modes: no IDs, no token, 404, 401, timeout, connection error
- chorus_transcripts.json always written (sentinel or real data) — never absent after script runs
- Authorization header is raw token with no prefix (CLAUDE.md non-negotiable requirement met)
- 401 distinguishably logged as WARNING (not swallowed silently like 404)
- write_dlq called before SystemExit(1) in the unexpected-exception path
- chorus_transcripts.json sentinel format matches: {"transcript_available": false, "conversations": []}
</success_criteria>

<output>
After completion, create .planning/phases/02-data-fetch/02-02-SUMMARY.md
</output>
