---
phase: 02-data-fetch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/fetch_hubspot.py
autonomous: true
requirements:
  - HUB-01
  - HUB-02
  - HUB-03
  - HUB-04
  - HUB-05
  - HUB-06
  - HUB-07
  - HUB-08

must_haves:
  truths:
    - "Running the script for a real test contact produces hubspot_contact.json in RUNNER_TEMP with all 18 contact properties and their updatedAt timestamps"
    - "email_engagements array contains all emails from the past 12 months with HTML stripped from bodies"
    - "meeting_engagements array is a deduplicated union of CRM search results and v4 associations batch-read results"
    - "deals array contains all associated deals with dealname, dealstage, amount, closedate, and updated_at"
    - "related_contacts_detail array contains sibling contacts at the same company (or empty array when no company association)"
    - "owner object contains firstName and lastName resolved from hubspot_owner_id"
    - "Every engagement object, deal object, and sibling contact object has an updated_at field (ISO string)"
    - "On unrecovered failure, write_dlq is called before SystemExit(1)"
  artifacts:
    - path: "scripts/fetch_hubspot.py"
      provides: "Full HubSpot data fetch — properties, emails, meetings (both paths), deals, related contacts, owner"
      exports: ["main"]
      min_lines: 200
  key_links:
    - from: "scripts/fetch_hubspot.py"
      to: "$RUNNER_TEMP/hubspot_contact.json"
      via: "file_io.write_json('hubspot_contact.json', output)"
      pattern: "write_json.*hubspot_contact"
    - from: "fetch_meeting_ids_v4 results"
      to: "meeting_engagements list"
      via: "deduplicate by id, batch-read new IDs, merge with search results"
      pattern: "batch_api.read.*meetings"
    - from: "contact.properties.get('associatedcompanyid')"
      to: "fetch_related_contacts(client, CONTACT_ID, company_id)"
      via: "non-null check before call; empty list when null"
      pattern: "associatedcompanyid"
---

<objective>
Implement scripts/fetch_hubspot.py — the real HubSpot data fetch script that replaces the Phase 1 stub.

Purpose: Phase 3 token computation requires structured contact data with per-field freshness timestamps. This script is the sole data producer for everything upstream of the Claude call. Getting the data shape, pagination, deduplication, and updatedAt coverage correct here means no Phase 3 workaround is needed.

Output: scripts/fetch_hubspot.py that writes $RUNNER_TEMP/hubspot_contact.json containing contact properties, email engagements, meeting engagements (CRM search + v4 associations deduplicated), deals, related contacts, and owner name — all with updatedAt timestamps.
</objective>

<execution_context>
@C:\Users\irahfo\Outreach\Asotos\.planning\phases\02-data-fetch\02-RESEARCH.md
</execution_context>

<context>
@C:\Users\irahfo\Outreach\Asotos\.planning\ROADMAP.md
@C:\Users\irahfo\Outreach\Asotos\CLAUDE.md

<interfaces>
<!-- Contracts the executor needs. Extracted from Phase 1 outputs. -->

From scripts/lib/api_client.py (created in Phase 1, Plan 02):
  hubspot_retry — tenacity decorator; retries ApiException with status in (429, 500, 502, 503, 504)
  Usage: @hubspot_retry on any function that calls the HubSpot SDK

From scripts/lib/file_io.py (created in Phase 1, Plan 02):
  write_json(filename: str, data: dict) -> None
    Writes to os.environ.get("RUNNER_TEMP", ".") / filename
    Creates parent dirs, utf-8, indent=2, ensure_ascii=False
  read_json(filename: str) -> dict
    Raises FileNotFoundError if absent

From scripts/lib/dlq_writer.py (created in Phase 1, Plan 02):
  write_dlq(contact_id: str, contact_email: str, failed_step: str, error_message: str) -> None
    Writes {contact_id, contact_email, failed_step, error_message[:2000], timestamp} to RUNNER_TEMP/failed_contacts.json

HubSpot client init (CLAUDE.md — no retry kwarg; hubspot_retry decorator owns retry):
  from hubspot import HubSpot
  client = HubSpot(access_token=os.environ["HUBSPOT_API_KEY"])

Environment variables consumed:
  HUBSPOT_API_KEY — HubSpot Private App token (required; KeyError if absent)
  CONTACT_ID      — HubSpot numeric contact ID (required; str)
  CONTACT_EMAIL   — Contact email (required; used in DLQ only)

hubspot_contact.json output schema (Phase 3 will consume this exactly):
  {
    "contact_id": str,
    "fetched_at": ISO_string,
    "properties": {
      property_name: value,   # all 18 properties as flat dict (string values)
      ...
    },
    "properties_updated_at": {
      # per-property timestamps for freshness-critical fields
      "name_of_target_role": ISO_string_or_null,
      "jobtitle": ISO_string_or_null,
      "name_of_target_role_last_updated": ISO_string_or_null,
    },
    "contact_updated_at": ISO_string_or_null,
    "contact_created_at": ISO_string_or_null,
    "email_engagements": [
      {
        "id": str,
        "hs_timestamp": ISO_string,
        "hs_email_subject": str,
        "hs_email_text": str,      # body_text — HTML stripped if needed
        "hs_email_direction": str,
        "hs_email_status": str,
        "updated_at": ISO_string_or_null,
      },
      ...
    ],
    "meeting_engagements": [
      {
        "id": str,
        "hs_timestamp": ISO_string_or_null,
        "hs_meeting_title": str,
        "hs_meeting_body": str,           # notes field
        "hs_internal_meeting_notes": str, # internal notes field
        "hs_meeting_start_time": ISO_string_or_null,
        "hs_meeting_end_time": ISO_string_or_null,
        "hs_meeting_outcome": str,
        "updated_at": ISO_string_or_null,
      },
      ...
    ],
    "deals": [
      {
        "id": str,
        "dealname": str,
        "dealstage": str,
        "amount": str,
        "closedate": str,
        "updated_at": ISO_string_or_null,
      },
      ...
    ],
    "related_contacts_detail": [
      {
        "id": str,
        "firstname": str,
        "lastname": str,
        "jobtitle": str,
        "email": str,
        "hs_lead_status": str,
        "updated_at": ISO_string_or_null,
      },
      ...
    ],
    "owner": {
      "id": str,
      "firstName": str,
      "lastName": str,
      "email": str,
    } | null
  }
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Verify SDK import paths and implement helper functions</name>
  <files>scripts/fetch_hubspot.py</files>
  <read_first>
    - C:\Users\irahfo\Outreach\Asotos\.planning\phases\02-data-fetch\02-RESEARCH.md (Pattern 1 through Pattern 8 — all code; Anti-Patterns; Pitfall 1 pagination loop; Pitfall 2 to_object_id cast; Pitfall 5 updated_at scoping; Open Questions 2 on SDK import paths)
    - C:\Users\irahfo\Outreach\Asotos\CLAUDE.md (Critical Implementation Rules — HubSpot Notes, HTML stripping rule, DLQ Pattern)
    - C:\Users\irahfo\Outreach\Asotos\.planning\phases\01-scaffold-trigger\01-02-PLAN.md (interfaces section — confirmed lib module signatures)
  </read_first>
  <action>
    Create scripts/fetch_hubspot.py. The script replaces the Phase 1 stub. Structure follows the RESEARCH.md "Complete fetch_hubspot.py Script Structure" code example exactly, with the helper functions below implemented as module-level functions before main().

    STEP 0 — Verify SDK import paths before writing final code. Run in a subprocess or inline check:
      python -c "from hubspot.crm.objects.emails import PublicObjectSearchRequest; print('emails OK')"
      python -c "from hubspot.crm.objects.meetings import PublicObjectSearchRequest; print('meetings OK')"
    If either import fails, use the generic path: "from hubspot.crm.objects import PublicObjectSearchRequest" with object_type parameter passed to the search call. Log which path succeeded so the SUMMARY records the verified import.

    FILE HEADER (exact):
      #!/usr/bin/env python3
      """fetch_hubspot.py — Phase 2: fetch HubSpot contact data and write hubspot_contact.json."""

    IMPORTS (in this order):
      import os, sys, datetime
      sys.path.insert(0, os.path.join(os.path.dirname(__file__), "lib"))
      from api_client import hubspot_retry
      from dlq_writer import write_dlq
      from file_io import write_json
      from hubspot import HubSpot
      from hubspot.crm.contacts.exceptions import ApiException
      from bs4 import BeautifulSoup

    MODULE-LEVEL CONSTANTS:
      CONTACT_ID = os.environ.get("CONTACT_ID", "")
      CONTACT_EMAIL = os.environ.get("CONTACT_EMAIL", "")
      TWELVE_MONTHS_AGO_MS = str(int(
          (datetime.datetime.now(datetime.timezone.utc) - datetime.timedelta(days=365)).timestamp() * 1000
      ))

    HELPER: strip_html(html_body: str) -> str
      If not html_body: return ""
      soup = BeautifulSoup(html_body, "lxml")
      return soup.get_text(separator=" ", strip=True)

    HELPER: _dt_to_iso(dt) -> str or None
      If dt is None: return None
      If hasattr(dt, 'isoformat'): return dt.isoformat()
      return str(dt)

    FUNCTION: fetch_contact_properties(client, contact_id) -> tuple[contact, contact_history]
      Decorated with @hubspot_retry.
      Call 1 — all 18 properties plus associatedcompanyid and createdate:
        client.crm.contacts.basic_api.get_by_id(
            contact_id=contact_id,
            properties=["firstname", "lastname", "email", "jobtitle", "company", "industry",
                        "numberofemployees", "city", "country", "website", "hubspot_owner_id",
                        "name_of_target_role", "name_of_target_role_last_updated",
                        "company_locations", "years_in_crm", "outreach_attempt_count",
                        "related_contacts", "secondary_contact_name",
                        "associatedcompanyid", "createdate"],
        )
      Call 2 — per-property timestamps for freshness-critical fields:
        client.crm.contacts.basic_api.get_by_id(
            contact_id=contact_id,
            properties_with_history=["name_of_target_role", "jobtitle", "name_of_target_role_last_updated"],
        )
      Return both objects as a tuple. Do NOT merge calls into one — HubSpot batch read does not support both parameters simultaneously (RESEARCH.md Pitfall 5 and Anti-Patterns).

    FUNCTION: fetch_email_engagements(client, contact_id) -> list
      Decorated with @hubspot_retry.
      Use RESEARCH.md Pattern 3 verbatim — cursor-based while True loop with paging.next.after check.
      Properties requested: ["hs_timestamp", "hs_email_subject", "hs_email_text", "hs_email_html", "hs_email_direction", "hs_email_status"]
      Filter: associations.contact EQ contact_id AND hs_timestamp GTE TWELVE_MONTHS_AGO_MS
      Pagination cap: if iteration count exceeds 50, log a WARNING and break (do not silently drop; warn first).
      Import PublicObjectSearchRequest from the verified path (Step 0).

    FUNCTION: fetch_meeting_engagements(client, contact_id) -> list
      Decorated with @hubspot_retry.
      Identical structure to fetch_email_engagements but targeting meetings object type.
      Properties requested: ["hs_timestamp", "hs_meeting_title", "hs_meeting_body", "hs_internal_meeting_notes",
                             "hs_meeting_start_time", "hs_meeting_end_time", "hs_meeting_outcome",
                             "hs_meeting_external_url", "hubspot_owner_id"]
      Filter: associations.contact EQ contact_id AND hs_timestamp GTE TWELVE_MONTHS_AGO_MS
      Import PublicObjectSearchRequest from the verified meeting path (Step 0).

    FUNCTION: fetch_meeting_ids_v4(client, contact_id) -> list[str]
      Decorated with @hubspot_retry.
      Use RESEARCH.md Pattern 5 — cursor while loop.
      Returns list of str(result.to_object_id) — always cast to str (Pitfall 2).

    FUNCTION: batch_read_meetings(client, meeting_ids: list[str], properties: list[str]) -> list
      Import BatchReadInputSimplePublicObjectId from hubspot.crm.objects.meetings.
      If meeting_ids is empty, return [].
      inputs = [{"id": mid} for mid in meeting_ids]  (already str from fetch_meeting_ids_v4)
      Call client.crm.objects.meetings.batch_api.read(...) and return resp.results.

    FUNCTION: merge_meetings(client, search_results: list, v4_ids: list[str]) -> list
      search_ids = {r.id for r in search_results}
      new_ids = [mid for mid in v4_ids if mid not in search_ids]
      if new_ids:
          meeting_props = ["hs_timestamp", "hs_meeting_title", "hs_meeting_body",
                           "hs_internal_meeting_notes", "hs_meeting_start_time",
                           "hs_meeting_end_time", "hs_meeting_outcome"]
          new_results = batch_read_meetings(client, new_ids, meeting_props)
          return list(search_results) + list(new_results)
      return list(search_results)

    FUNCTION: fetch_deals(client, contact_id) -> list
      Decorated with @hubspot_retry on the associations page call.
      Use RESEARCH.md Pattern 6 verbatim — get deal IDs via v4 associations, then batch-read.
      Always str(result.to_object_id) on ID extraction.
      Properties for batch-read: ["dealname", "dealstage", "amount", "closedate", "pipeline"]
      Return [] if no deal IDs found.

    FUNCTION: fetch_related_contacts(client, contact_id, company_id) -> list
      Decorated with @hubspot_retry on the associations page call.
      Use RESEARCH.md Pattern 7 verbatim — get sibling contact IDs excluding contact_id, batch-read up to 100.
      Always str(result.to_object_id) on ID extraction.
      Properties for batch-read: ["firstname", "lastname", "jobtitle", "email", "hs_lead_status", "lastmodifieddate"]
      If company_id is None or falsy, skip and return [].

    FUNCTION: fetch_owner(client, owner_id) -> dict or None
      Use RESEARCH.md Pattern 8 verbatim.
      owner_id cast to int: int(owner_id).
      Return None (not raise) on any exception — owner lookup is non-fatal.
      Log warning on failure: print(f"[fetch_hubspot] WARNING: owner fetch failed for owner_id={owner_id}")

    FUNCTION: assemble_output(contact, contact_history, emails, meetings, deals, related, owner) -> dict
      Build the hubspot_contact.json structure as specified in the interfaces section of this plan.

      properties: {k: v for k, v in contact.properties.items() if k not in ("associatedcompanyid", "createdate")}
        Note: keep associatedcompanyid and createdate out of the output properties dict — they are internal plumbing fields; Phase 3 uses contact_created_at for years_in_crm computation instead.

      properties_updated_at: Extract from contact_history.properties_with_history (if attribute exists):
        For each key in ["name_of_target_role", "jobtitle", "name_of_target_role_last_updated"]:
          history_list = contact_history.properties_with_history.get(key, []) if contact_history.properties_with_history else []
          Use history_list[0]["timestamp"] as the ISO string if list is non-empty; else None.

      contact_updated_at: _dt_to_iso(contact.updated_at)
      contact_created_at: _dt_to_iso(contact.created_at)
      fetched_at: datetime.datetime.now(datetime.timezone.utc).isoformat()

      email_engagements: For each email result:
        body_text = email.properties.get("hs_email_text") or strip_html(email.properties.get("hs_email_html") or "")
        {
          "id": email.id,
          "hs_timestamp": email.properties.get("hs_timestamp"),
          "hs_email_subject": email.properties.get("hs_email_subject") or "",
          "hs_email_text": body_text,
          "hs_email_direction": email.properties.get("hs_email_direction") or "",
          "hs_email_status": email.properties.get("hs_email_status") or "",
          "updated_at": _dt_to_iso(email.updated_at),
        }

      meeting_engagements: For each meeting result:
        {
          "id": meeting.id,
          "hs_timestamp": meeting.properties.get("hs_timestamp"),
          "hs_meeting_title": meeting.properties.get("hs_meeting_title") or "",
          "hs_meeting_body": meeting.properties.get("hs_meeting_body") or "",
          "hs_internal_meeting_notes": meeting.properties.get("hs_internal_meeting_notes") or "",
          "hs_meeting_start_time": meeting.properties.get("hs_meeting_start_time"),
          "hs_meeting_end_time": meeting.properties.get("hs_meeting_end_time"),
          "hs_meeting_outcome": meeting.properties.get("hs_meeting_outcome") or "",
          "updated_at": _dt_to_iso(meeting.updated_at),
        }

      deals: For each deal result:
        {
          "id": deal.id,
          "dealname": deal.properties.get("dealname") or "",
          "dealstage": deal.properties.get("dealstage") or "",
          "amount": deal.properties.get("amount") or "",
          "closedate": deal.properties.get("closedate") or "",
          "updated_at": _dt_to_iso(deal.updated_at),
        }

      related_contacts_detail: For each sibling result:
        {
          "id": sibling.id,
          "firstname": sibling.properties.get("firstname") or "",
          "lastname": sibling.properties.get("lastname") or "",
          "jobtitle": sibling.properties.get("jobtitle") or "",
          "email": sibling.properties.get("email") or "",
          "hs_lead_status": sibling.properties.get("hs_lead_status") or "",
          "updated_at": _dt_to_iso(sibling.updated_at),
        }

      owner: The dict from fetch_owner or None.

    FUNCTION: main()
      Per RESEARCH.md "Complete fetch_hubspot.py Script Structure":
        client = HubSpot(access_token=os.environ["HUBSPOT_API_KEY"])
        print(f"[fetch_hubspot] fetching contact_id={CONTACT_ID}")
        contact, contact_history = fetch_contact_properties(client, CONTACT_ID)
        emails = fetch_email_engagements(client, CONTACT_ID)
        meetings_search = fetch_meeting_engagements(client, CONTACT_ID)
        meeting_ids_v4 = fetch_meeting_ids_v4(client, CONTACT_ID)
        meetings = merge_meetings(client, meetings_search, meeting_ids_v4)
        deals = fetch_deals(client, CONTACT_ID)
        company_id = contact.properties.get("associatedcompanyid")
        related = fetch_related_contacts(client, CONTACT_ID, company_id) if company_id else []
        owner_id = contact.properties.get("hubspot_owner_id")
        owner = fetch_owner(client, owner_id) if owner_id else None
        output = assemble_output(contact, contact_history, emails, meetings, deals, related, owner)
        write_json("hubspot_contact.json", output)
        print(f"[fetch_hubspot] wrote hubspot_contact.json "
              f"({len(emails)} emails, {len(meetings)} meetings, {len(deals)} deals)")

    if __name__ == "__main__" guard (per RESEARCH.md structure):
        try: main()
        except Exception as e:
            write_dlq(CONTACT_ID, CONTACT_EMAIL, "fetch_hubspot", str(e))
            raise SystemExit(1)

    SECURITY RULES (per RESEARCH.md Security Domain and Anti-Patterns):
    - Never log HUBSPOT_API_KEY or any value from os.environ that contains "KEY" or "TOKEN"
    - In except blocks for ApiException, log only e.status (e.g., print(f"HubSpot API error: status={e.status}")) — never log str(e) or repr(e) which may include auth headers
    - Log contact_id freely; log contact_email only in the DLQ record (not in print statements)
  </action>
  <verify>
    <automated>cd /mnt/c/Users/irahfo/Outreach/Asotos && python -c "
import ast, sys
with open('scripts/fetch_hubspot.py') as f:
    source = f.read()
# Syntax check
try:
    ast.parse(source)
    print('syntax OK')
except SyntaxError as e:
    print(f'SYNTAX ERROR: {e}')
    sys.exit(1)
# Check required function names exist
required = ['fetch_contact_properties', 'fetch_email_engagements', 'fetch_meeting_engagements',
            'fetch_meeting_ids_v4', 'batch_read_meetings', 'merge_meetings',
            'fetch_deals', 'fetch_related_contacts', 'fetch_owner', 'assemble_output', 'main']
for fn in required:
    assert fn in source, f'Missing function: {fn}'
print('all required functions present')
# Check DLQ guard
assert 'write_dlq' in source, 'Missing write_dlq call'
assert 'SystemExit(1)' in source, 'Missing SystemExit(1)'
print('DLQ guard present')
# Check no hardcoded RUNNER_TEMP path
assert '/home/runner' not in source, 'Hardcoded runner path found'
assert 'write_json' in source, 'Missing write_json call'
print('file_io pattern OK')
# Check updatedAt coverage
assert '_dt_to_iso' in source, 'Missing _dt_to_iso helper'
assert 'updated_at' in source, 'No updated_at field in output assembly'
print('updatedAt helper present')
# Verify no top-level hubspot/requests import (lazy imports only in _is_retryable_*)
import re
lines = source.splitlines()
top_imports = [l for l in lines[:30] if re.match(r'^(import|from) (requests|anthropic)\b', l)]
assert not top_imports, f'Top-level requests/anthropic import found: {top_imports}'
print('no prohibited top-level imports')
print('Task 1 verify PASSED')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - scripts/fetch_hubspot.py passes ast.parse (no syntax errors)
    - All 11 functions present: fetch_contact_properties, fetch_email_engagements, fetch_meeting_engagements, fetch_meeting_ids_v4, batch_read_meetings, merge_meetings, fetch_deals, fetch_related_contacts, fetch_owner, assemble_output, main
    - @hubspot_retry decorator applied to: fetch_contact_properties, fetch_email_engagements, fetch_meeting_engagements, fetch_meeting_ids_v4 (and within fetch_deals and fetch_related_contacts on their associations page calls)
    - v4 associations to_object_id always cast to str() before use in batch read inputs or set membership checks
    - Email pagination and meeting pagination both use while True / paging.next.after cursor loops with a 50-page cap WARNING (not silent stop)
    - strip_html uses BeautifulSoup(html, "lxml").get_text(separator=" ", strip=True); falls back to html.parser if lxml is unavailable
    - assemble_output produces all top-level keys: contact_id, fetched_at, properties, properties_updated_at, contact_updated_at, contact_created_at, email_engagements, meeting_engagements, deals, related_contacts_detail, owner
    - write_dlq called before SystemExit(1) in the __main__ guard
    - No hardcoded /home/runner or RUNNER_TEMP path string in file — uses write_json from file_io
    - HUBSPOT_API_KEY never logged; ApiException logged via e.status only
  </acceptance_criteria>
  <done>scripts/fetch_hubspot.py exists, passes syntax check, contains all required functions with correct structure, and produces the full hubspot_contact.json schema when run against a real HubSpot contact</done>
</task>

<task type="auto">
  <name>Task 2: Smoke-test against a real contact and verify hubspot_contact.json structure</name>
  <files>scripts/fetch_hubspot.py</files>
  <read_first>
    - C:\Users\irahfo\Outreach\Asotos\.planning\phases\02-data-fetch\02-RESEARCH.md (Open Questions 2 on SDK import paths; Open Questions 3 on associatedcompanyid; Assumptions Log A2 on to_object_id type; Pitfall 3 on CRM search vs scheduler meetings)
    - C:\Users\irahfo\Outreach\Asotos\CLAUDE.md (Known Risks section — HubSpot notes scope, Chorus endpoint uncertainty)
  </read_first>
  <action>
    This task validates Task 1 output against the real HubSpot API and fixes any runtime errors found.

    STEP 1 — Verify SDK import paths. Run:
      python -c "from hubspot.crm.objects.emails import PublicObjectSearchRequest; print('emails import OK')"
      python -c "from hubspot.crm.objects.meetings import PublicObjectSearchRequest; print('meetings import OK')"
    If either fails, edit scripts/fetch_hubspot.py to use the fallback import path (generic hubspot.crm.objects with object_type parameter). Record the working path in the task output comment at the top of the file.

    STEP 2 — Verify propertiesWithHistory SDK support. Run:
      python -c "
      import hubspot; help(hubspot.crm.contacts.BasicApiApi.get_by_id)
      " 2>&1 | grep -i "properties_with_history" | head -5
    If properties_with_history is not a supported parameter name, fall back to storing only contact.updated_at for all properties (set all three keys in properties_updated_at to None and note this in a comment). Log the finding.

    STEP 3 — Run the script against a real test contact:
      Set environment: HUBSPOT_API_KEY from the repo secret (must be provided in the local .env or shell), CONTACT_ID to a known HubSpot contact ID, CONTACT_EMAIL to that contact's email, RUNNER_TEMP to a temp dir.
      Run: python scripts/fetch_hubspot.py
      Capture exit code and any printed output.

    STEP 4 — Validate hubspot_contact.json output:
      Run the following structural checks:
        python -c "
        import json, os
        runner_temp = os.environ.get('RUNNER_TEMP', '.')
        with open(os.path.join(runner_temp, 'hubspot_contact.json')) as f:
            data = json.load(f)
        required_keys = ['contact_id', 'fetched_at', 'properties', 'properties_updated_at',
                         'contact_updated_at', 'contact_created_at', 'email_engagements',
                         'meeting_engagements', 'deals', 'related_contacts_detail', 'owner']
        missing = [k for k in required_keys if k not in data]
        assert not missing, f'Missing top-level keys: {missing}'
        # Check 18 contact properties present
        required_props = ['firstname', 'lastname', 'email', 'jobtitle', 'company', 'industry',
                          'numberofemployees', 'city', 'country', 'website', 'hubspot_owner_id',
                          'name_of_target_role', 'name_of_target_role_last_updated',
                          'company_locations', 'years_in_crm', 'outreach_attempt_count',
                          'related_contacts', 'secondary_contact_name']
        missing_props = [p for p in required_props if p not in data['properties']]
        assert not missing_props, f'Missing properties: {missing_props}'
        # Check email_engagements structure
        for e in data['email_engagements']:
            for field in ['id', 'hs_timestamp', 'hs_email_subject', 'hs_email_text', 'updated_at']:
                assert field in e, f'Email missing field {field}: {e}'
        # Check meeting_engagements structure
        for m in data['meeting_engagements']:
            for field in ['id', 'hs_meeting_body', 'hs_internal_meeting_notes', 'updated_at']:
                assert field in m, f'Meeting missing field {field}: {m}'
        # Check deals structure
        for d in data['deals']:
            for field in ['id', 'dealname', 'dealstage', 'updated_at']:
                assert field in d, f'Deal missing field {field}: {d}'
        print(f'hubspot_contact.json structure OK')
        print(f'  emails: {len(data[\"email_engagements\"])}')
        print(f'  meetings: {len(data[\"meeting_engagements\"])}')
        print(f'  deals: {len(data[\"deals\"])}')
        print(f'  related_contacts: {len(data[\"related_contacts_detail\"])}')
        print(f'  owner: {data[\"owner\"]}')
        "

    STEP 5 — Fix any issues found in Steps 3-4. Common issues to check and fix:
      - If SDK import path fails: update import statement in fetch_hubspot.py to the working path
      - If propertiesWithHistory raises TypeError: switch to None fallback for properties_updated_at values
      - If to_object_id type causes batch read failure: add explicit str() cast (should already be there)
      - If pagination returns 0 results but contact has known engagements: check filter syntax — associations.contact must be a string value, not integer

    If the script cannot be run against a live HubSpot contact in this environment (no secret available locally), document that in a comment in the file header and mark this task as requiring CI validation. The structural check (STEP 4) can be performed against a manually constructed fixture JSON to confirm the assertion logic is correct.
  </action>
  <verify>
    <automated>cd /mnt/c/Users/irahfo/Outreach/Asotos && python -c "
import json, os, sys

# If HUBSPOT_API_KEY is set, validate live output; otherwise validate fixture
runner_temp = os.environ.get('RUNNER_TEMP', '.')
output_path = os.path.join(runner_temp, 'hubspot_contact.json')

if not os.path.exists(output_path):
    # Create a minimal fixture to validate assertion logic
    fixture = {
        'contact_id': 'test',
        'fetched_at': '2026-06-12T00:00:00+00:00',
        'properties': {k: '' for k in ['firstname','lastname','email','jobtitle','company','industry',
                                        'numberofemployees','city','country','website','hubspot_owner_id',
                                        'name_of_target_role','name_of_target_role_last_updated',
                                        'company_locations','years_in_crm','outreach_attempt_count',
                                        'related_contacts','secondary_contact_name']},
        'properties_updated_at': {'name_of_target_role': None, 'jobtitle': None, 'name_of_target_role_last_updated': None},
        'contact_updated_at': None,
        'contact_created_at': None,
        'email_engagements': [{'id': '1', 'hs_timestamp': None, 'hs_email_subject': '', 'hs_email_text': '', 'hs_email_direction': '', 'hs_email_status': '', 'updated_at': None}],
        'meeting_engagements': [{'id': '2', 'hs_timestamp': None, 'hs_meeting_title': '', 'hs_meeting_body': '', 'hs_internal_meeting_notes': '', 'hs_meeting_start_time': None, 'hs_meeting_end_time': None, 'hs_meeting_outcome': '', 'updated_at': None}],
        'deals': [{'id': '3', 'dealname': '', 'dealstage': '', 'amount': '', 'closedate': '', 'updated_at': None}],
        'related_contacts_detail': [],
        'owner': None
    }
    with open(output_path, 'w') as f:
        json.dump(fixture, f, indent=2)
    print('No live output found — wrote fixture for schema validation')

with open(output_path) as f:
    data = json.load(f)

required_keys = ['contact_id','fetched_at','properties','properties_updated_at',
                 'contact_updated_at','contact_created_at','email_engagements',
                 'meeting_engagements','deals','related_contacts_detail','owner']
missing_keys = [k for k in required_keys if k not in data]
assert not missing_keys, f'Missing keys: {missing_keys}'

required_props = ['firstname','lastname','email','jobtitle','company','industry',
                  'numberofemployees','city','country','website','hubspot_owner_id',
                  'name_of_target_role','name_of_target_role_last_updated',
                  'company_locations','years_in_crm','outreach_attempt_count',
                  'related_contacts','secondary_contact_name']
missing_props = [p for p in required_props if p not in data['properties']]
assert not missing_props, f'Missing properties: {missing_props}'

required_updated_at_keys = ['name_of_target_role','jobtitle','name_of_target_role_last_updated']
missing_ts = [k for k in required_updated_at_keys if k not in data['properties_updated_at']]
assert not missing_ts, f'Missing per-property timestamps: {missing_ts}'

for e in data['email_engagements']:
    for field in ['id','hs_email_text','updated_at']:
        assert field in e, f'Email missing {field}'

for m in data['meeting_engagements']:
    for field in ['id','hs_meeting_body','hs_internal_meeting_notes','updated_at']:
        assert field in m, f'Meeting missing {field}'

for d in data['deals']:
    for field in ['id','dealname','dealstage','updated_at']:
        assert field in d, f'Deal missing {field}'

print('hubspot_contact.json schema validation PASSED')
print(f'emails={len(data[\"email_engagements\"])}, meetings={len(data[\"meeting_engagements\"])}, deals={len(data[\"deals\"])}, related={len(data[\"related_contacts_detail\"])}')
"
    </automated>
  </verify>
  <acceptance_criteria>
    - Script exits 0 when run with valid HUBSPOT_API_KEY, CONTACT_ID, CONTACT_EMAIL, and RUNNER_TEMP set
    - hubspot_contact.json contains all 11 top-level keys listed in the interfaces section
    - properties dict contains all 18 required HubSpot property names
    - properties_updated_at contains keys for name_of_target_role, jobtitle, name_of_target_role_last_updated (values may be null if no history available)
    - Each email_engagement item contains: id, hs_timestamp, hs_email_subject, hs_email_text (HTML stripped), hs_email_direction, hs_email_status, updated_at
    - Each meeting_engagement item contains: id, hs_timestamp, hs_meeting_title, hs_meeting_body, hs_internal_meeting_notes, hs_meeting_start_time, hs_meeting_end_time, hs_meeting_outcome, updated_at
    - Each deal item contains: id, dealname, dealstage, amount, closedate, updated_at
    - Each related_contact item contains: id, firstname, lastname, jobtitle, email, hs_lead_status, updated_at
    - Engagement counts in the JSON match what is visible in the HubSpot contact timeline (spot-check by the developer)
    - Schema validation assertion script passes without AssertionError
  </acceptance_criteria>
  <done>fetch_hubspot.py runs end-to-end against a real HubSpot contact; hubspot_contact.json passes all structural assertions; engagement count spot-check matches HubSpot timeline</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions env → script | CONTACT_ID from workflow_dispatch input; used in HubSpot API calls as path/filter parameter |
| HubSpot API response → output JSON | API responses serialised directly into file; contact data is trusted (Staff Domain's own CRM) |
| scripts/fetch_hubspot.py → $RUNNER_TEMP | File write goes to runner-controlled temp directory; path from RUNNER_TEMP env var |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Information Disclosure | fetch_hubspot.py exception handlers | mitigate | Catch ApiException and log only e.status — never log str(e) or repr(e) which includes auth headers in HubSpot SDK exceptions; HUBSPOT_API_KEY never referenced in print statements |
| T-02-02 | Tampering | CONTACT_ID in API filter value | mitigate | CONTACT_ID is passed as the "value" field in an associations.contact EQ filter — the HubSpot SDK serialises this as a JSON string value, not interpolated into a URL template; no injection path exists |
| T-02-03 | Information Disclosure | CONTACT_EMAIL in log output | mitigate | contact_email logged only in DLQ record (RUNNER_TEMP/failed_contacts.json, not stdout); all print statements use contact_id only |
| T-02-04 | Denial of Service | Pagination loop with no cap | mitigate | 50-page cap with WARNING log on both email and meeting cursor loops; prevents runaway loops on contacts with unusually large engagement history from consuming entire job timeout |
| T-02-05 | Elevation of Privilege | HUBSPOT_API_KEY scope | accept | Private App token scope is set at HubSpot portal level; script uses whatever scope the token has; scope restriction is the operator's responsibility (not enforceable in script code) |
</threat_model>

<verification>
After both tasks complete:
1. python -c "import ast; ast.parse(open('scripts/fetch_hubspot.py').read()); print('syntax OK')" — must print "syntax OK"
2. Run schema validation assertion from Task 2 verify block — must print "hubspot_contact.json schema validation PASSED"
3. Spot-check: count email_engagements in the JSON against the HubSpot contact timeline for the test contact — counts must match
4. Confirm no API key in log output: run script and grep stdout for any string containing the first 4 chars of HUBSPOT_API_KEY — should return 0 matches
5. python -c "s=open('scripts/fetch_hubspot.py').read(); assert '/home/runner' not in s; assert 'write_json' in s; print('path pattern OK')"
</verification>

<success_criteria>
- scripts/fetch_hubspot.py exists with all 11 required functions
- Script exits 0 against a real test contact
- hubspot_contact.json passes all structural assertions (all 11 top-level keys, all 18 properties, per-field updatedAt for 3 freshness-critical fields)
- Engagement counts match HubSpot timeline (spot-check)
- write_dlq called before SystemExit(1) in exception handler
- No secrets logged in stdout
</success_criteria>

<output>
After completion, create .planning/phases/02-data-fetch/02-01-SUMMARY.md
</output>
